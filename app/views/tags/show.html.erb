<div class="container">
  <%= turbo_frame_tag dom_id( @tag ) do %>
    <div class="container">
      <h2><em><%= "#{@tag.title}" %>...</em></h2>
      <hr/>
      <%= render @post %>
    </div>
  <% end %>
</div>
