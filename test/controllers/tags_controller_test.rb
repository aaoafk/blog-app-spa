# frozen_string_literal: true

require 'test_helper'

class TagsControllerTest < ActionDispatch::IntegrationTest
  test 'the tags are listed in index' do
    skip 'implement Tags#index test'
  end

  test 'tag can be shown with Tags#show' do
    skip 'implement Tags#show test '
  end

  test 'tag can be destroyed with Tags#destroy' do
    skip 'implement Tags#destroy test'
  end

  test 'tag can be updated with Tags#update' do
    skip 'determine whether this test is needed'
  end
end
